# Never commit proprietary or prep-provider-derived content
packs/far-becker-mega.json
packs/far-aicpa-2026.json
packs/private/
private/
_personal/

# Editor / OS junk
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Node / build (if added later)
node_modules/
dist/
build/

# Env / secrets
.env
.env.local
*.key

# Paid products (sold on Gumroad — must NEVER be committed to the public repo)
far-silent-defaults-pro.html
FAR-Silent-Defaults-PRO.pdf
*-pro.html
